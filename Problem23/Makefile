problemx = problem23

build:
	@mkdir -p bin
	@gcc -Wall -Wextra -O3 -o bin/${problemx} main.c
	@echo 'Finished compiling'
	@echo 'Execute with `./bin/${problemx}`'

clean:
	@rm -rf bin
	@echo 'bin/ deleted'
